<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lego Collection</title>
  <link rel="stylesheet" href="/css/main.css" />

</head>

<body tabindex="-1">

  <%- include('partials/navbar', {page: ''}) %>

  <div class="container mx-auto p-4">

    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center ">
        <div class="max-w-md">
          <h1 class="text-5xl font-bold">Edit Set</h1>

          <p class="py-6">Fill the form to update <%= set.name %> information</p>
        </div>
      </div>
    </div>

    <br />
    <form method="post" action="/lego/editSet">
        <label class="input input-bordered flex items-center gap-2" style="color: #6E0B75; font-weight: bold;">
            Updated Name:
            <input type="text" class="grow" placeholder="Gears" required value="<%= set.name %>"/>
        </label>
        <br />
        <label class="input input-bordered flex items-center gap-2" style="color: #6E0B75; font-weight: bold;">
            Updated Year:
            <input type="number" class="grow" placeholder="2024" required value="<%= set.year %>"/>
        </label>
        <br />
        <label class="input input-bordered flex items-center gap-2" style="color: #6E0B75; font-weight: bold;">
            Updated Number of Parts:
            <input type="number" class="grow" placeholder="100" required value="<%= set.num_parts %>"/>
        </label>
        <br />
        <label class="input input-bordered flex items-center gap-2" style="color: #6E0B75; font-weight: bold;">
            Updated Image Links (URL): 
            <input type="url" class="grow" placeholder="https://cdn.rebrickable.com/media/sets/001-1.jpg" required value="<%= set.img_url %>"/>
        </label>
        <br />
        <label class="input input-bordered flex items-center gap-2" style="color: #6E0B75; font-weight: bold;">
            Updated Theme:
        <select class="select select-primary w-full max-w-xs" name="theme_id" required>
            <% themes.forEach(theme=>{ %>
                <option <%= (set.theme_id == theme.id) ? "selected" : "" %> value="<%= theme.id %>">
                  <%= theme.name %>
                </option>
              <% }) %>
              
        </select>
        </label>
        <br />

        <label class="input input-bordered flex items-center gap-2" style="color: #6E0B75; font-weight: bold;">
            Set Number
            <input type="text" class="grow" placeholder="001-1" required value="<%= set.set_num %>" class="cursor-not-allowed focus:cursor-auto" readonly/>
        </label>
        <br />
        <br />
        <button type="submit" class="btn btn-outline btn-success">Update</button>
        <a href="/lego/deleteSet/<%= set.set_num %>" class="btn btn-outline btn-error">Delet</a>
    </form>

</body>

</html>